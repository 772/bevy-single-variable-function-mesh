[package]
name = "bevy-single-variable-function-mesh"
version = "0.13.0"
authors = ["Armin Schäfer"]
description = "A 2D or 3D mesh (`bevy::render::mesh::Mesh`) generated from a single-variable function `f(f32) -> f32`."
repository = "https://github.com/772/bevy-single-variable-function-mesh"
license = "MIT OR Apache-2.0"
readme = "README.md"
edition = "2021"
categories = ["rendering"]
keywords = ["graphics"]

[dependencies]
bevy = { version = "0.13", default-features = false, features = ["bevy_render"] }
